[pytest]
DJANGO_SETTINGS_MODULE = backend.settings
python_files = tests.py test_*.py *_tests.py
addopts = --reuse-db --strict-markers --maxfail=1 -q --cov=backend --cov-report=term-missing
testpaths =
    core
    users
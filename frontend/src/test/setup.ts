/**
 * Vitest global setup — runs before all tests.
 *
 * Imports @testing-library/jest-dom to add custom DOM matchers like
 * .toBeInTheDocument(), .toBeVisible(), etc.
 */
import '@testing-library/jest-dom'
